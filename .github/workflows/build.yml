name: Build & Publish

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            args: --win
          - os: ubuntu-latest
            args: --linux AppImage deb rpm tar.gz
          - os: macos-latest
            args: --mac --universal
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Test
        run: npm test

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Fetch ffmpeg
        uses: actions/cache@v5
        id: ffmpeg-cache
        with:
          path: resources/ffmpeg
          key: ffmpeg-7.1-${{ runner.os }}-${{ runner.arch }}

      - name: Download ffmpeg if not cached
        if: steps.ffmpeg-cache.outputs.cache-hit != 'true'
        run: npm run fetch:ffmpeg

      # Universal mac (--universal) needs BOTH FFmpeg arches bundled: the
      # process.arch differs at runtime on Intel (x64) vs Apple Silicon
      # (arm64). The arm64 macos runner cached darwin-arm64 above; pull the
      # x64 build too. Gated to macOS — Win/Linux runners would otherwise
      # waste ~120MB downloading a binary they never ship, and each run only
      # writes its own <platform>-<arch> subdir so this never clobbers arm64.
      - name: Download x64 ffmpeg for universal mac
        if: runner.os == 'macOS' && steps.ffmpeg-cache.outputs.cache-hit != 'true'
        run: npm run fetch:ffmpeg -- --platform darwin --arch x64

      # Universal mac packs x64 + arm64 from the SAME node_modules, which only
      # holds the host-arch sharp binary — the merge then fails on the
      # identical Mach-O (see electron-builder.yml x64ArchFiles), and worse,
      # Intel Macs would ship the arm64 binary. Stage the x64 sharp package
      # directly (self-contained, no deps); `npm install --cpu=x64` would
      # reify and prune the arm64 twin. Version follows sharp's own
      # optionalDependencies so it never rots on dependabot bumps.
      - name: Stage x64 sharp for universal mac
        if: runner.os == 'macOS'
        run: |
          SHARP_X64_VER=$(node -p "require('./node_modules/sharp/package.json').optionalDependencies['@img/sharp-darwin-x64']")
          npm pack "@img/sharp-darwin-x64@$SHARP_X64_VER" --pack-destination "$RUNNER_TEMP"
          mkdir -p node_modules/@img/sharp-darwin-x64
          tar -xzf "$RUNNER_TEMP"/img-sharp-darwin-x64-*.tgz -C node_modules/@img/sharp-darwin-x64 --strip-components=1

      - name: Build & Publish
        run: npx electron-builder ${{ matrix.args }} --publish always
        env:
          # electron-builder's github publisher requires this (see
          # electron-builder.yml `publish`); release.yml already uses the same
          # repo-scoped secret for tags/PRs.
          GH_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Smoke test installer (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $v = & "${{ github.workspace }}\dist\win-unpacked\onda.exe" --version
          echo "version: $v"

      - name: Smoke test unpacked app (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          chmod +x "${{ github.workspace }}/dist/linux-unpacked/onda"
          "${{ github.workspace }}/dist/linux-unpacked/onda" --version
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GH_OWNER: ${{ github.repository_owner }}
          # Code signing is OFF until certificates are provisioned. See
          # RELEASE.md §6 and electron-builder.yml (win.publisherName /
          # verifyUpdateCodeSignature, mac.notarize).
          #
          # Do NOT add empty CSC_LINK/CSC_KEY_PASSWORD vars here — electron-builder
          # treats even an empty CSC_LINK as a cert path and fails (macOS: "not a
          # file"). When certs are available, add the real secrets and:
          #   - Windows: CSC_LINK, CSC_KEY_PASSWORD (or CODE_SIGN_PUBLISHER for
          #     publisherName + verifyUpdateCodeSignature: true).
          #   - macOS: CSC_LINK, CSC_KEY_PASSWORD, APPLE_ID,
          #     APPLE_APP_SPECIFIC_PASSWORD, APPLE_TEAM_ID (notarize: true).
          CSC_IDENTITY_AUTO_DISCOVERY: false
